# 새 세션 인계 메모 (2026-08-23)

> ⏰ **오늘 자정(24:00)까지 과제 제출.** 이 문서는 새 세션 시작하자마자 먼저 읽을 것.
> 전체 기획은 `CONCEPT.md` 참고, 여긴 "지금 뭐가 되고 뭐가 안 되는지 + 오늘 밤 뭘 해야 하는지"만 정리.

## 프로젝트 한 줄 요약
**냥냥집** — 반려묘 집사를 위한 인터랙티브 사이트. 고양이 방 그림을 드래그로 둘러보고, 사물(6개) 클릭하면 제품/콘텐츠로 연결. 주소는 `pointer-blob` 저장소 그대로(GitHub Pages + Vercel 자동배포).

## 지금 파일 상태
- `CONCEPT.md` — 전체 기획서 (목적, 사물 목록, 확정된 결정사항, 방식 변경 이력 다 정리돼있음)
- `index.html` / `style.css` / `script.js` — 실제 동작하는 뼈대 (드래그 이동, 스크롤 확대, 클릭 하이라이트+안내문구)
- `room-bg.png` — 최종 배경 그림 1장 (1774×887px). 지피티로 생성, 스타일 확정본.
- `layout-sketch.svg` — 배치 참고용 스케치 (더 이상 그림 생성엔 안 씀, 기록용)
- `room-sketch-1/2/3.svg` — **더 이상 안 씀** (3분할 이어붙이기 시도했다가 색감/수평선 안 맞아서 포기, 단일 이미지로 전환함. 삭제해도 되고 그냥 둬도 됨)

## ⚠️ 지금 안 되는 것 (오늘 밤 최우선으로 확인/고칠 것)

1. **드래그하면 화면이 투명하게 찢어지는 것처럼 보임** (브라우저 기본 "드래그 고스트" 효과가 우리 커스텀 드래그랑 충돌).
   → `script.js`의 pointerdown 핸들러에 `e.preventDefault()` + `dragstart` 이벤트 막기, `style.css`의 `.viewport`에 `touch-action:none; user-select:none;` **추가는 이미 해뒀는데(커밋 안 된 상태), 실제로 고쳐졌는지 브라우저에서 재확인을 못 했음.** 제일 먼저 이것부터 테스트할 것.
2. **오른쪽 끝에서 사물 클릭하면 왼쪽으로 다시 이동이 안 됨** — 위 1번과 같은 원인일 가능성 높음(1번 고쳐지면 같이 풀릴 수 있음). 1번 확인 후 같이 재확인.
3. **클릭 영역이 사물 모양이 아니라 큰 사각형으로 뜸** — `style.css`의 `.hotspot { border-radius: 14px; }`를 그대로 둔 상태. 사물 모양에 더 가깝게 하려면 `border-radius: 50%`(타원)로 바꾸고 글로우 크기(box-shadow 블러값)도 줄이는 게 빠른 개선. **완벽한 윤곽선 추적은 사물마다 배경 뗀 이미지가 따로 있어야 가능**(지금 없음) — 오늘 밤엔 시간상 무리이니 타원 근사로 마무리하는 걸 권장.
4. **클릭 영역 좌표가 눈대중** — `index.html`의 각 `.hotspot` style의 left/top/width/height %가 실제 `room-bg.png` 사물 위치와 정확히 안 맞을 수 있음. 브라우저에서 열어서 눈으로 보고 어긋난 만큼 숫자 조정 필요.

## 확정되어 더 이상 안 물어봐도 되는 것 (CONCEPT.md에도 있음)
- 고양이 1마리, 사물 6개(사료그릇/고양이/캣타워/TV/창문+새/장난감), 사이트 이름 냥냥집
- 좌우 드래그만 지원, 무한 루프 아님(끝에서 멈춤)
- 확대는 CSS scale로 흉내만 냄 (진짜 레이어 원근감 아님)
- 링크는 지금 다 가짜(토스트 안내문구)임 — 실제 상품/영상 URL 아직 없음. 오늘 제출용이면 이 상태로도 괜찮을 가능성 높음(발표 시 "실제 서비스라면 이렇게 연결됩니다"로 설명 가능)

## 오늘 조사해서 알게 된 것 (참고용, 다시 검토 안 해도 됨)
레퍼런스였던 balmingtiger.com은 **krpano라는 상용 VR 파노라마 엔진**을 쓰는 사이트였음(실제 코드 확인함). 우리가 만드는 CSS/JS 기반 드래그 방식은 그것의 "합리적으로 축소한 버전"이지 잘못된 방향이 아님 — 이 부분으로 다시 방향 고민할 필요 없음.

## 오늘 밤 할 일 순서 (제출 마감 고려한 우선순위)
1. `index.html`을 브라우저로 열어서 위 1~2번(드래그 고스트, 오른쪽 끝 버그) 실제로 고쳐졌는지 확인
2. 안 고쳐졌으면 원인 다시 찾기 (같은 세션 이어서 진행 중이던 작업)
3. 클릭 영역을 타원형으로 바꾸고, 6개 좌표를 실제 그림 보면서 눈대중으로 재조정
4. 전체 플로우 한 번씩 다 눌러보고 테스트 (드래그 양방향, 확대, 6개 클릭 다)
5. 커밋 (커밋 메시지는 항상 먼저 챙겨줄 것 — 사용자가 매번 안 물어봐도 되게)
6. 시간 남으면: 커서 모양, 실제 배포 링크(`https://adam-park.github.io/pointer-blob/`, `https://pointer-blob.vercel.app/`) 열어서 실제로 잘 보이는지 최종 확인

## 사용자 관련 참고
- 비개발자, 코드/터미널 작업은 Claude가 대신 함. git 커밋은 VSCode 소스 제어에서 본인이 직접 — **매번 커밋 메시지 텍스트를 먼저 제시할 것** (물어볼 때까지 기다리지 말 것)
- 오늘 과제 제출이 걸려있어서 마음이 급함. 완벽함보다 "허접해 보이지 않는" 최소 완성도가 목표.
